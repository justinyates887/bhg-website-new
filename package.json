{
    "scripts": {
        "build": "cd client && npm run build",
        "install-client": "cd client && npm install",
        "install-backend": "cd backend && npm install",
        "heroku-postbuild": "npm run install-client && npm run build && install-backend",
        "start": "cd backend && node ."
    }
}